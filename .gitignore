.DS_Store
._*
node_modules/
*.log
.env

# Pacote 05A — fonte primária de terceiros: manter privada, nunca publicar no repo público.
# Ver SOURCE_RIGHTS_NOTICE.md. Os ficheiros ficam localmente para consulta interna.
sources/primary/private/
apps/visual-source-board/assets/pages/

# Pacote 05F — segredos, fontes privadas e artefactos de Supabase (nunca no Git público).
.env.*
!.env.example
.private-assets/
sources/**/private/
supabase/.branches/
supabase/.temp/
data/working/
data/private/
*.dump
*.sql.gz

# Pacote 07A — output de build (não commitar)
dist/

# Pacote 08A — override local do runtime colaborativo (pode conter chaves)
public/config/collaborative-area.runtime.local.json
