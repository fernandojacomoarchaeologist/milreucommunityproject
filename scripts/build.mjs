/**
 * © 2026 Fernando Rodrigues de Jácomo.
 * Produzido no âmbito do Projeto Comunitário de Milreu.
 * Consultar RIGHTS.md.
 */
import { cp, mkdir, rm, writeFile } from "node:fs/promises";
import { createHash } from "node:crypto";
import { readFileSync } from "node:fs";
import { buildStaticRecordPages } from "./structured/static-record-builder.mjs";
await rm("dist",{recursive:true,force:true});await mkdir("dist",{recursive:true});
for(const item of ["index.html","404.html",".nojekyll","src","public","auth"]){await cp(item,`dist/${item}`,{recursive:true})}
const notificationModelData=readFileSync("public/data/collaborative-notification-model.json");const notificationModelHash=createHash("sha256").update(notificationModelData).digest("hex");const notificationTemplatesData=readFileSync("public/data/collaborative-notification-templates.json");const notificationTemplatesHash=createHash("sha256").update(notificationTemplatesData).digest("hex");const notificationRuntimeData=readFileSync("public/config/notifications.runtime.json");const notificationRuntimeHash=createHash("sha256").update(notificationRuntimeData).digest("hex");const deploymentProfileData=readFileSync("public/config/deployment-profile.runtime.json");const deploymentProfileHash=createHash("sha256").update(deploymentProfileData).digest("hex");const deploymentReadinessData=readFileSync("public/data/deployment-readiness.json");const deploymentReadinessHash=createHash("sha256").update(deploymentReadinessData).digest("hex");const homologationModelData=readFileSync("public/data/collaborative-homologation-model.json");const homologationModelHash=createHash("sha256").update(homologationModelData).digest("hex");const reviewModelData=readFileSync("public/data/collaborative-museum-review-model.json");const reviewModelHash=createHash("sha256").update(reviewModelData).digest("hex");const trainingData=readFileSync("public/data/collaborative-training-trails.json");const trainingHash=createHash("sha256").update(trainingData).digest("hex");const libraryData=readFileSync("public/data/collaborative-library.json");const libraryHash=createHash("sha256").update(libraryData).digest("hex");const editorialApprovedData=readFileSync("public/data/museum-editorial-approved.json");const editorialApprovedHash=createHash("sha256").update(editorialApprovedData).digest("hex");const publicEffectsData=readFileSync("public/data/public-content-effects.json");const publicEffectsHash=createHash("sha256").update(publicEffectsData).digest("hex");const impactRegistryData=readFileSync("public/data/package-impact-registry.json");const impactRegistryHash=createHash("sha256").update(impactRegistryData).digest("hex");const contributionSummaryData=readFileSync("public/data/contributions-public-summary.json");const contributionSummaryHash=createHash("sha256").update(contributionSummaryData).digest("hex");const contributionModelData=readFileSync("public/data/collaborative-contribution-model.json");const contributionModelHash=createHash("sha256").update(contributionModelData).digest("hex");const exhibitionPublicData=readFileSync("public/data/exhibitions-public.json");const exhibitionPublicHash=createHash("sha256").update(exhibitionPublicData).digest("hex");const exhibitionModelData=readFileSync("public/data/collaborative-exhibition-model.json");const exhibitionModelHash=createHash("sha256").update(exhibitionModelData).digest("hex");const collaborativeConfig=readFileSync("public/config/collaborative-area.runtime.json");const collaborativeConfigHash=createHash("sha256").update(collaborativeConfig).digest("hex");const taskModelData=readFileSync("public/data/collaborative-task-model.json");const taskModelHash=createHash("sha256").update(taskModelData).digest("hex");const collaborativeData=readFileSync("public/data/collaborative-modules.json");const collaborativeHash=createHash("sha256").update(collaborativeData).digest("hex");const carouselData=readFileSync("public/data/home-carousel.json");const carouselHash=createHash("sha256").update(carouselData).digest("hex");const channelsData=readFileSync("public/data/channels/channel-records.json");const channelsHash=createHash("sha256").update(channelsData).digest("hex");const structuredData=readFileSync("public/data/structured/museum-dataset.jsonld");const structuredHash=createHash("sha256").update(structuredData).digest("hex");const data=readFileSync("public/data/memories.json");const hash=createHash("sha256").update(data).digest("hex");const collections=readFileSync("public/data/museum-collections.json");const collectionsHash=createHash("sha256").update(collections).digest("hex");const index=readFileSync("public/data/museum-index.json");const indexHash=createHash("sha256").update(index).digest("hex");
await buildStaticRecordPages("dist");
await writeFile("dist/build-manifest.json",JSON.stringify({_copyright:"© 2026 Fernando Rodrigues de Jácomo — Projeto Comunitário de Milreu",version:"0.19.0",builtAt:new Date().toISOString(),memoriesChecksum:`sha256:${hash}`,collectionsChecksum:`sha256:${collectionsHash}`,indexChecksum:`sha256:${indexHash}`,collaborativeConfigChecksum:`sha256:${collaborativeConfigHash}`,taskModelChecksum:`sha256:${taskModelHash}`,exhibitionModelChecksum:`sha256:${exhibitionModelHash}`,publicExhibitionsChecksum:`sha256:${exhibitionPublicHash}`,contributionModelChecksum:`sha256:${contributionModelHash}`,contributionSummaryChecksum:`sha256:${contributionSummaryHash}`,museumReviewModelChecksum:`sha256:${reviewModelHash}`,trainingTrailsChecksum:`sha256:${trainingHash}`,libraryChecksum:`sha256:${libraryHash}`,museumEditorialApprovedChecksum:`sha256:${editorialApprovedHash}`,publicContentEffectsChecksum:`sha256:${publicEffectsHash}`,packageImpactRegistryChecksum:`sha256:${impactRegistryHash}`,deploymentProfileChecksum:`sha256:${deploymentProfileHash}`,deploymentReadinessChecksum:`sha256:${deploymentReadinessHash}`,homologationModelChecksum:`sha256:${homologationModelHash}`,notificationModelChecksum:`sha256:${notificationModelHash}`,notificationTemplatesChecksum:`sha256:${notificationTemplatesHash}`,notificationRuntimeChecksum:`sha256:${notificationRuntimeHash}`,collaborativeChecksum:`sha256:${collaborativeHash}`,carouselChecksum:`sha256:${carouselHash}`,channelsChecksum:`sha256:${channelsHash}`,structuredChecksum:`sha256:${structuredHash}`,mode:"editorial-preview-noindex"},null,2)+"\n");
console.log("Build concluído em dist/.");
